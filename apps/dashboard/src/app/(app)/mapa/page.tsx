'use client';

/**
 * Vista de mapa limpio. El mapa ya vive en (app)/layout.tsx como fondo
 * universal — esta ruta sólo existe para mostrarlo sin paneles encima.
 */
export default function MapPage() {
  return null;
}
